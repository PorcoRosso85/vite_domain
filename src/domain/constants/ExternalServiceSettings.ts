export const ExternalServiceSettings = {
  apiTimeout: 15000,
  apiRetryCount: 3,
  dbRetryLimit: 5,
  dbRetryDelay: 3000,
  maxApiRateLimit: 1000,
  maxApiResponseSize: 10 * 1024 * 1024, // 10 MB
};
